{
  "name": "kshetra",
  "version": "0.0.0",
  "private": true,
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/types build && bun run --cwd apps/api build && bun run --cwd apps/web build",
    "check": "bun run --cwd packages/types build && bun run --cwd apps/api check && bun run --cwd apps/web check",
    "dev:api": "bun run --cwd packages/types build && bun run --cwd apps/api start:dev",
    "dev:web": "bun run --cwd packages/types build && bun run --cwd apps/web dev",
    "prisma:generate": "prisma generate --schema prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema prisma/schema.prisma",
    "prisma:seed": "bun prisma/seed.ts",
    "prisma:studio": "prisma studio --schema prisma/schema.prisma"
  },
  "devDependencies": {
    "prisma": "6.19.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@prisma/client": "6.19.2"
  }
}
